# ============================================================================
# LOCAL DEVELOPMENT (SQLite)
# ============================================================================
# For local development, set DATABASE_TYPE=sqlite
# The app will use a local sqlite.db file (no external database needed)

DATABASE_TYPE=sqlite

# ============================================================================
# PRODUCTION (Neon Postgres on Vercel)
# ============================================================================
# For production deployment on Vercel, set:
# - DATABASE_TYPE=postgres (or remove DATABASE_TYPE entirely)
# - POSTGRES_URL=your-neon-connection-string
# - POSTGRES_HOST, POSTGRES_USER, POSTGRES_PASSWORD (optional overrides)

# Database configuration
# DATABASE_TYPE=sqlite  # Uncomment for local SQLite, comment out for Neon Postgres

# Neon Serverless Postgres (for Vercel deployment)
# Create a free database at https://neon.tech
# Go to Connection Details and copy the connection string
# IMPORTANT: Add ?sslmode=require at the end
# POSTGRES_URL=postgres://username:password@ep-xxx.region.neon.tech/mini_store_inv_app?sslmode=require

# Individual Neon connection parameters (used when DATABASE_TYPE is not sqlite)
# POSTGRES_HOST=ep-xxx.region.neon.tech
# POSTGRES_USER=your_neon_username
# POSTGRES_PASSWORD=your_neon_password
# POSTGRES_DATABASE=mini_store_inv_app

# API URL (change for production)
# VITE_API_URL=https://your-api-domain.com
